<svg
  class="HoverArrow"
  width="10"
  height="10"
  viewBox="0 0 10 10"
  aria-hidden="true"
>
  <g fill-rule="evenodd">
    <path class="HoverArrow__linePath" d="M0 5h7"></path>
    <path class="HoverArrow__tipPath" d="M1 1l4 4-4 4"></path>
  </g>
</svg>

<style>
  .HoverArrow {
    overflow: visible;
    fill: none;
    stroke: currentColor; /* Will inherit the text colour from the parent element */
    stroke-width: 2px; /* Defines the stroke width */
    position: relative;
  }

  .HoverArrow__linePath {
    opacity: 0;
    transition: opacity 150ms cubic-bezier(0.215, 0.61, 0.355, 1);
  }

  .HoverArrow__tipPath {
    transition: transform 150ms cubic-bezier(0.215, 0.61, 0.355, 1);
  }

  :global(a:hover) .HoverArrow__linePath {
    opacity: 1; /* Show the line */
  }

  :global(a:hover) .HoverArrow__tipPath {
    transform: translateX(0.225rem); /* Move the arrow tip to the right */
  }

  /* Reverse animation on active (when clicked or touched) */
  :global(a:active) .HoverArrow__linePath {
    opacity: 0; /* Hide the line */
  }

  :global(a:active) .HoverArrow__tipPath {
    transform: translateX(0); /* Move arrow tip back to original position */
  }
</style>
