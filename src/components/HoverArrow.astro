<svg
  width="10"
  height="10"
  viewBox="0 0 10 10"
  aria-hidden="true"
  focusable="false"
  role="img"
>
  <g fill="none" stroke="currentColor" stroke-width="2">
    <path class="hover-arrow__line" d="M0 5h7"></path>
    <path class="hover-arrow__tip" d="M1 1l4 4-4 4"></path>
  </g>
</svg>

<style>
  svg {
    overflow: visible;
    position: relative;
  }

  .hover-arrow__line {
    opacity: 0;
    transition: opacity 150ms cubic-bezier(0.215, 0.61, 0.355, 1);
  }

  .hover-arrow__tip {
    transition: transform 150ms cubic-bezier(0.215, 0.61, 0.355, 1);
  }

  /* Button states: using :global to apply styles globally */
  :global(a:hover) .hover-arrow__line,
  :global(a:focus-visible) .hover-arrow__line {
    opacity: 1;
  }

  :global(a:hover) .hover-arrow__tip,
  :global(a:focus-visible) .hover-arrow__tip {
    transform: translateX(0.225rem);
  }

  :global(a:active) .hover-arrow__line {
    opacity: 0;
  }

  :global(a:active) .hover-arrow__tip {
    transform: translateX(0);
  }
</style>
