---
interface Props {
  text: string;
  class?: string;
}

const { text, class: _class } = Astro.props;
---

<h2 class=`${_class} font-0 font-semibold` style="color: var(--color_link);">
  {text}
</h2>
